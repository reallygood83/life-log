# Life Log AI Analysis Feature Specification

> **Version**: 1.0.0
> **Last Updated**: 2026-01-18
> **Status**: Draft

---

## 1. 개요 (Overview)

### 1.1 목적
Life Log 플러그인에 AI 기반 분석 기능을 추가하여, 사용자의 학습/운동/업무/식단 기록을 분석하고 인사이트를 제공하는 리포트를 생성한다.

### 1.2 핵심 기능
- **다중 AI Provider 지원**: Gemini, OpenAI, Grok, OpenRouter
- **카테고리별 분석**: 4개 기록 유형에 맞춤화된 분석
- **템플릿 시스템**: 기본 템플릿 제공 + 사용자 정의 템플릿 지원
- **리포트 생성**: Markdown 형식의 분석 리포트 자동 생성

---

## 2. 지원 AI Provider

### 2.1 Provider 목록

| Provider | API Key 설정 | 기본 모델 | 비고 |
|----------|-------------|----------|------|
| **OpenAI** | `openaiApiKey` | gpt-4o | GPT-4, GPT-4o, GPT-4o-mini 선택 가능 |
| **Gemini** | `geminiApiKey` | gemini-2.0-flash | gemini-1.5-pro, gemini-2.0-flash 등 |
| **Grok** | `grokApiKey` | grok-3 | xAI의 Grok 모델 |
| **OpenRouter** | `openRouterApiKey` | 사용자 선택 | 다양한 모델 지원, 모델 ID 직접 입력 |

### 2.2 OpenRouter 특별 지원
```typescript
interface OpenRouterConfig {
  apiKey: string;
  defaultModel: string;  // e.g., "anthropic/claude-3.5-sonnet"
  availableModels: string[];  // 사용자가 추가한 모델 목록
}
```

OpenRouter에서 자주 사용되는 모델 예시:
- `anthropic/claude-3.5-sonnet`
- `anthropic/claude-3-opus`
- `google/gemini-pro-1.5`
- `meta-llama/llama-3.1-405b-instruct`
- `mistralai/mistral-large`

---

## 3. 기록 카테고리별 분석 기능

### 3.1 Study Log (학습 기록) 분석

#### 3.1.1 수집 데이터
```typescript
interface StudyAnalysisData {
  period: DateRange;
  sessions: StudySession[];
  subjects: SubjectStats[];
  totalStudyTime: number;
  pomodoroCount: number;
  scores: SelfEvalScores[];
}
```

#### 3.1.2 기본 템플릿

| 템플릿 ID | 템플릿 이름 | 분석 내용 |
|-----------|------------|----------|
| `study-weekly` | 주간 학습 리포트 | 주간 학습 시간, 과목별 분포, 집중도 분석 |
| `study-monthly` | 월간 학습 리포트 | 월간 트렌드, 목표 달성률, 개선점 |
| `study-subject` | 과목별 분석 | 특정 과목의 학습 패턴 및 효율성 |
| `study-efficiency` | 학습 효율 분석 | 시간대별 집중도, 최적 학습 시간 추천 |
| `study-goal` | 목표 달성 분석 | 설정 목표 vs 실제 달성률, 개선 방안 |

#### 3.1.3 프롬프트 예시
```
당신은 학습 코칭 전문가입니다. 다음 학습 기록을 분석하여
{{templateType}}에 맞는 리포트를 작성해주세요.

[학습 기록 데이터]
{{studyData}}

[분석 요청사항]
{{analysisRequirements}}

[출력 형식]
Markdown 형식으로 다음 섹션을 포함해주세요:
1. 요약
2. 상세 분석
3. 강점
4. 개선점
5. 추천 액션 플랜
```

---

### 3.2 Life Log / Workout (운동 기록) 분석

#### 3.2.1 수집 데이터
```typescript
interface WorkoutAnalysisData {
  period: DateRange;
  workouts: ParsedWorkout[];
  exerciseStats: ExerciseStats[];
  totalDuration: number;
  completionRate: number;
  progressData: ProgressMetrics[];
}
```

#### 3.2.2 기본 템플릿

| 템플릿 ID | 템플릿 이름 | 분석 내용 |
|-----------|------------|----------|
| `workout-weekly` | 주간 운동 리포트 | 주간 운동량, 운동 유형 분포, 달성률 |
| `workout-monthly` | 월간 운동 리포트 | 월간 트렌드, 체력 변화 추이 |
| `workout-progress` | 진척도 분석 | 중량/횟수 증가 추이, 개인기록 |
| `workout-balance` | 운동 밸런스 분석 | 근육군별 운동 분포, 불균형 감지 |
| `workout-recovery` | 회복 패턴 분석 | 휴식일 분석, 과훈련 감지 |

---

### 3.3 Work Log (업무 기록) 분석

#### 3.3.1 수집 데이터
```typescript
interface WorkAnalysisData {
  period: DateRange;
  tasks: ParsedWorkLog[];
  categoryStats: CategoryStats[];
  totalWorkTime: number;
  overtimeCount: number;
  completionRate: number;
}
```

#### 3.3.2 기본 템플릿

| 템플릿 ID | 템플릿 이름 | 분석 내용 |
|-----------|------------|----------|
| `work-weekly` | 주간 업무 리포트 | 주간 업무 시간, 카테고리별 분포 |
| `work-monthly` | 월간 업무 리포트 | 월간 생산성 트렌드, 야근 패턴 |
| `work-productivity` | 생산성 분석 | 예상 vs 실제 소요시간, 효율성 |
| `work-category` | 카테고리별 분석 | 업무 유형별 시간 투자 분석 |
| `work-burnout` | 번아웃 위험 분석 | 과로 패턴 감지, 워라밸 점검 |

---

### 3.4 Meal Log (식단 기록) 분석

#### 3.4.1 수집 데이터
```typescript
interface MealAnalysisData {
  period: DateRange;
  meals: ParsedMealLog[];
  mealTypeStats: MealTypeStats[];
  foodFrequency: FoodFrequency[];
  regularityScore: number;
}
```

#### 3.4.2 기본 템플릿

| 템플릿 ID | 템플릿 이름 | 분석 내용 |
|-----------|------------|----------|
| `meal-weekly` | 주간 식단 리포트 | 주간 식사 패턴, 규칙성 분석 |
| `meal-monthly` | 월간 식단 리포트 | 월간 식습관 트렌드 |
| `meal-nutrition` | 영양 밸런스 분석 | 음식 다양성, 영양 균형 추정 |
| `meal-pattern` | 식사 패턴 분석 | 식사 시간대, 결식 패턴 |
| `meal-suggestion` | 식단 개선 제안 | 부족한 영양소 추정, 개선 제안 |

---

## 4. 사용자 정의 템플릿 시스템

### 4.1 템플릿 구조
```typescript
interface AnalysisTemplate {
  id: string;
  name: string;
  category: 'study' | 'workout' | 'work' | 'meal' | 'custom';
  description: string;
  isBuiltIn: boolean;
  isEnabled: boolean;

  // 프롬프트 설정
  systemPrompt: string;
  userPromptTemplate: string;

  // 데이터 수집 설정
  dataRange: 'day' | 'week' | 'month' | 'quarter' | 'custom';
  customDays?: number;

  // 출력 설정
  outputFormat: 'markdown' | 'bullet-points' | 'structured';
  outputSections: string[];

  // 메타데이터
  createdAt: string;
  updatedAt: string;
  usageCount: number;
}
```

### 4.2 템플릿 변수

템플릿에서 사용 가능한 변수:

| 변수 | 설명 | 예시 |
|------|------|------|
| `{{period}}` | 분석 기간 | "2026-01-01 ~ 2026-01-07" |
| `{{category}}` | 기록 카테고리 | "study", "workout" |
| `{{recordCount}}` | 기록 개수 | 15 |
| `{{totalDuration}}` | 총 시간 | "32시간 15분" |
| `{{rawData}}` | 원본 데이터 (JSON) | {...} |
| `{{summary}}` | 자동 생성 요약 | "이번 주 7회 학습..." |
| `{{userName}}` | 사용자 이름 | "김철수" |
| `{{currentDate}}` | 현재 날짜 | "2026-01-18" |

### 4.3 사용자 템플릿 관리 UI

```
┌─────────────────────────────────────────────────────────┐
│  📊 분석 템플릿 관리                                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [기본 템플릿]                                           │
│  ├─ 📚 학습                                             │
│  │   ├─ ✓ 주간 학습 리포트                              │
│  │   ├─ ✓ 월간 학습 리포트                              │
│  │   └─ □ 과목별 분석 (비활성화)                         │
│  ├─ 💪 운동                                             │
│  │   └─ ...                                            │
│  └─ ...                                                │
│                                                         │
│  [사용자 템플릿]                                         │
│  ├─ 🎯 나만의 주간 회고 템플릿                           │
│  └─ + 새 템플릿 추가                                    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 5. 설정 (Settings)

### 5.1 새로운 설정 항목

```typescript
interface AIAnalysisSettings {
  // Provider 설정
  defaultProvider: 'openai' | 'gemini' | 'grok' | 'openrouter';

  // API Keys
  openaiApiKey: string;
  openaiModel: string;  // gpt-4o, gpt-4o-mini, gpt-4-turbo

  geminiApiKey: string;
  geminiModel: string;  // gemini-2.0-flash, gemini-1.5-pro

  grokApiKey: string;
  grokModel: string;  // grok-3, grok-2

  openRouterApiKey: string;
  openRouterModel: string;  // 사용자 선택
  openRouterCustomModels: string[];  // 추가 모델 목록

  // 분석 설정
  autoAnalysis: boolean;  // 자동 분석 활성화
  analysisSchedule: 'daily' | 'weekly' | 'manual';

  // 템플릿 설정
  enabledTemplates: string[];  // 활성화된 템플릿 ID 목록
  customTemplates: AnalysisTemplate[];

  // 리포트 저장 설정
  reportSavePath: string;  // 기본: "LifeLog/Reports"
  reportNaming: string;  // 기본: "{{category}}_{{date}}_report"
}
```

### 5.2 설정 UI 레이아웃

```
┌─────────────────────────────────────────────────────────┐
│  ⚙️ Life Log Settings                                   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  🤖 AI 분석 설정                                         │
│  ─────────────────                                      │
│                                                         │
│  AI Provider                                            │
│  ┌──────────────────────────────────────┐               │
│  │ ○ OpenAI  ○ Gemini  ○ Grok  ● OpenRouter │          │
│  └──────────────────────────────────────┘               │
│                                                         │
│  ▼ OpenRouter 설정                                      │
│  ┌──────────────────────────────────────┐               │
│  │ API Key: ●●●●●●●●●●●●●●●●            │               │
│  │ 기본 모델: anthropic/claude-3.5-sonnet │              │
│  │                                       │               │
│  │ [+ 모델 추가]                          │               │
│  │ ├─ google/gemini-pro-1.5             │               │
│  │ └─ meta-llama/llama-3.1-405b         │               │
│  └──────────────────────────────────────┘               │
│                                                         │
│  ▶ OpenAI 설정 (접힘)                                   │
│  ▶ Gemini 설정 (접힘)                                   │
│  ▶ Grok 설정 (접힘)                                     │
│                                                         │
│  📊 리포트 설정                                          │
│  ─────────────────                                      │
│  저장 경로: [LifeLog/Reports        ]                   │
│  파일명 형식: [{{category}}_{{date}}]                   │
│                                                         │
│  📝 템플릿 관리                                          │
│  ─────────────────                                      │
│  [템플릿 관리 열기]                                      │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 6. UI/UX 설계

### 6.1 분석 실행 방법

#### 방법 1: 코드 블록 내 버튼
각 로그 타입의 렌더링된 UI에 "AI 분석" 버튼 추가

```
┌─────────────────────────────────────────┐
│  📚 오늘의 학습 기록                      │
│  ─────────────────────                  │
│  ✓ 수학 - 45분                          │
│  ✓ 영어 - 30분                          │
│  ─────────────────────                  │
│  [완료] [복사]  [🤖 AI 분석]             │
└─────────────────────────────────────────┘
```

#### 방법 2: 커맨드 팔레트
- `Life Log: AI 학습 분석 실행`
- `Life Log: AI 운동 분석 실행`
- `Life Log: AI 업무 분석 실행`
- `Life Log: AI 식단 분석 실행`
- `Life Log: 전체 분석 리포트 생성`

#### 방법 3: 사이드바 아이콘
리본 메뉴에 AI 분석 아이콘 추가 → 클릭 시 분석 모달 오픈

### 6.2 분석 모달

```
┌─────────────────────────────────────────────────────────┐
│  🤖 AI 분석 리포트 생성                          [X]    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  분석 대상                                              │
│  ┌──────────────────────────────────────┐               │
│  │ ☑ 학습 기록  ☑ 운동 기록              │               │
│  │ ☐ 업무 기록  ☐ 식단 기록              │               │
│  └──────────────────────────────────────┘               │
│                                                         │
│  기간 선택                                              │
│  ┌──────────────────────────────────────┐               │
│  │ ● 이번 주  ○ 이번 달  ○ 직접 선택     │               │
│  └──────────────────────────────────────┘               │
│                                                         │
│  템플릿 선택                                            │
│  ┌──────────────────────────────────────┐               │
│  │ 📊 주간 종합 리포트                   │ ▼            │
│  └──────────────────────────────────────┘               │
│                                                         │
│  AI 모델                                                │
│  ┌──────────────────────────────────────┐               │
│  │ OpenRouter: claude-3.5-sonnet        │ ▼            │
│  └──────────────────────────────────────┘               │
│                                                         │
│         [취소]                    [📊 분석 시작]         │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 6.3 분석 진행 상태

```
┌─────────────────────────────────────────────────────────┐
│  🤖 분석 진행 중...                              [X]    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ████████████░░░░░░░░░░░░░░░░░░  45%                   │
│                                                         │
│  ✓ 기록 데이터 수집 완료                                │
│  ✓ 데이터 전처리 완료                                   │
│  ◐ AI 분석 요청 중... (claude-3.5-sonnet)              │
│  ○ 리포트 생성                                         │
│  ○ 파일 저장                                           │
│                                                         │
│                              [취소]                     │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 6.4 리포트 미리보기

```
┌─────────────────────────────────────────────────────────┐
│  📊 분석 리포트 - 2026년 1월 2주차                [X]    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  # 주간 학습 분석 리포트                                 │
│                                                         │
│  ## 📈 요약                                             │
│  이번 주 총 학습 시간: **12시간 30분**                   │
│  학습 세션: 15회 | 평균 집중 시간: 50분                  │
│                                                         │
│  ## 📚 과목별 분석                                      │
│  | 과목 | 시간 | 비율 |                                 │
│  |------|------|------|                                 │
│  | 수학 | 5h   | 40%  |                                 │
│  | 영어 | 4h   | 32%  |                                 │
│  ...                                                    │
│                                                         │
│  ## 💡 개선 제안                                        │
│  1. 오후 2-4시 집중력이 가장 높습니다                    │
│  2. 과학 과목 학습 시간 증가 권장                        │
│  ...                                                    │
│                                                         │
├─────────────────────────────────────────────────────────┤
│  [다시 생성]    [클립보드 복사]    [📄 노트로 저장]       │
└─────────────────────────────────────────────────────────┘
```

---

## 7. 기술 아키텍처

### 7.1 파일 구조

```
src/
├── ai/
│   ├── providers/
│   │   ├── base.ts           # AIProvider 인터페이스
│   │   ├── openai.ts         # OpenAI 구현
│   │   ├── gemini.ts         # Gemini 구현
│   │   ├── grok.ts           # Grok 구현
│   │   └── openrouter.ts     # OpenRouter 구현
│   │
│   ├── templates/
│   │   ├── base.ts           # 템플릿 베이스 클래스
│   │   ├── study.ts          # 학습 분석 템플릿
│   │   ├── workout.ts        # 운동 분석 템플릿
│   │   ├── work.ts           # 업무 분석 템플릿
│   │   └── meal.ts           # 식단 분석 템플릿
│   │
│   ├── collectors/
│   │   ├── base.ts           # DataCollector 인터페이스
│   │   ├── study.ts          # 학습 데이터 수집
│   │   ├── workout.ts        # 운동 데이터 수집
│   │   ├── work.ts           # 업무 데이터 수집
│   │   └── meal.ts           # 식단 데이터 수집
│   │
│   ├── analyzer.ts           # 분석 오케스트레이터
│   ├── report-generator.ts   # 리포트 생성기
│   └── index.ts              # AI 모듈 진입점
│
├── modal/
│   ├── AIAnalysisModal.ts    # 분석 설정 모달
│   ├── TemplateEditorModal.ts # 템플릿 편집 모달
│   └── ReportPreviewModal.ts # 리포트 미리보기 모달
│
└── settings/
    └── AISettingsTab.ts      # AI 설정 탭
```

### 7.2 핵심 인터페이스

```typescript
// AI Provider 인터페이스
interface AIProvider {
  name: string;
  isConfigured(): boolean;
  analyze(prompt: string, options?: AnalysisOptions): Promise<string>;
  getAvailableModels(): string[];
  testConnection(): Promise<boolean>;
}

// 분석 옵션
interface AnalysisOptions {
  model?: string;
  temperature?: number;
  maxTokens?: number;
  systemPrompt?: string;
}

// 데이터 수집기 인터페이스
interface DataCollector<T> {
  collectData(dateRange: DateRange): Promise<T[]>;
  summarize(data: T[]): DataSummary;
  toPromptFormat(data: T[]): string;
}

// 분석 요청
interface AnalysisRequest {
  category: LogCategory;
  template: AnalysisTemplate;
  dateRange: DateRange;
  provider: AIProvider;
  model?: string;
}

// 분석 결과
interface AnalysisResult {
  success: boolean;
  content: string;
  metadata: {
    provider: string;
    model: string;
    tokensUsed: number;
    generatedAt: string;
  };
  error?: string;
}
```

### 7.3 분석 흐름

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   User      │────▶│  Analyzer   │────▶│  Collector  │
│  Request    │     │ Orchestrator│     │  (by type)  │
└─────────────┘     └─────────────┘     └─────────────┘
                           │                   │
                           │                   ▼
                           │            ┌─────────────┐
                           │            │  Raw Data   │
                           │            │  from Vault │
                           │            └─────────────┘
                           │                   │
                           ▼                   ▼
                    ┌─────────────┐     ┌─────────────┐
                    │  Template   │◀────│  Processed  │
                    │  Engine     │     │    Data     │
                    └─────────────┘     └─────────────┘
                           │
                           ▼
                    ┌─────────────┐
                    │    Prompt   │
                    │  Generation │
                    └─────────────┘
                           │
                           ▼
                    ┌─────────────┐
                    │ AI Provider │
                    │   (API)     │
                    └─────────────┘
                           │
                           ▼
                    ┌─────────────┐
                    │   Report    │
                    │  Generator  │
                    └─────────────┘
                           │
                           ▼
                    ┌─────────────┐
                    │  Save to    │
                    │   Vault     │
                    └─────────────┘
```

---

## 8. 데이터 수집 상세

### 8.1 Vault에서 기록 수집

```typescript
class VaultDataCollector {
  async findLogBlocks(
    category: LogCategory,
    dateRange: DateRange
  ): Promise<LogBlock[]> {
    // 1. Vault의 모든 마크다운 파일 순회
    // 2. 해당 카테고리의 코드 블록 찾기
    // 3. 날짜 범위 내의 기록만 필터링
    // 4. 파싱된 데이터 반환
  }
}
```

### 8.2 카테고리별 데이터 스키마

#### Study Log
```typescript
{
  title: string;
  subject: string;
  startDate: string;
  duration: string;
  tasks: { name: string; state: string; duration?: string }[];
  scores?: { understanding: number; effort: number; satisfaction: number };
  pomodoroCount?: number;
}
```

#### Workout Log
```typescript
{
  title: string;
  startDate: string;
  duration: string;
  exercises: { name: string; state: string; params: { key: string; value: string }[] }[];
  completionRate: number;
}
```

#### Work Log
```typescript
{
  title: string;
  category: string;
  startDate: string;
  duration: string;
  expectedDuration?: string;
  tasks: { name: string; state: string; duration?: string }[];
}
```

#### Meal Log
```typescript
{
  title: string;
  mealType: string;
  date: string;
  foods: { name: string; state: string }[];
  photo?: string;
  notes?: string;
}
```

---

## 9. 보안 고려사항

### 9.1 API Key 보안
- API 키는 Obsidian의 `data.json`에 저장 (플러그인 표준)
- 민감한 설정은 별도의 `.env` 파일 지원 고려
- API 키 마스킹 표시 (설정 UI에서)

### 9.2 데이터 프라이버시
- 분석에 전송되는 데이터 최소화
- 개인정보 제거 옵션 (이름, 특정 키워드 마스킹)
- 로컬 분석 옵션 (Ollama 지원 고려)

### 9.3 Rate Limiting
- API 호출 횟수 제한 관리
- 에러 처리 및 재시도 로직
- 사용량 추적 및 알림

---

## 10. 개발 로드맵

### Phase 1: 기반 구축 (1-2주)
- [ ] AI Provider 추상화 레이어 구현
- [ ] OpenAI, Gemini Provider 구현
- [ ] 기본 설정 UI 추가
- [ ] API 연결 테스트 기능

### Phase 2: 데이터 수집 (1주)
- [ ] Vault 데이터 수집기 구현
- [ ] 4개 카테고리별 데이터 파서
- [ ] 날짜 범위 필터링

### Phase 3: 템플릿 시스템 (1주)
- [ ] 기본 템플릿 구현 (카테고리당 3개씩)
- [ ] 템플릿 변수 치환 엔진
- [ ] 사용자 템플릿 CRUD

### Phase 4: 분석 및 리포트 (1주)
- [ ] 분석 오케스트레이터 구현
- [ ] 리포트 생성기
- [ ] 분석 모달 UI

### Phase 5: Grok & OpenRouter (1주)
- [ ] Grok Provider 구현
- [ ] OpenRouter Provider 구현
- [ ] 커스텀 모델 선택 UI

### Phase 6: 고급 기능 (1-2주)
- [ ] 자동 분석 스케줄링
- [ ] 분석 히스토리 관리
- [ ] 리포트 비교 기능
- [ ] 템플릿 공유/가져오기

---

## 11. 테스트 계획

### 11.1 단위 테스트
- Provider API 호출 테스트
- 데이터 수집기 파싱 테스트
- 템플릿 변수 치환 테스트

### 11.2 통합 테스트
- 전체 분석 흐름 테스트
- 다중 Provider 전환 테스트
- 대용량 데이터 처리 테스트

### 11.3 사용자 테스트
- 설정 UI 사용성
- 분석 결과 품질
- 에러 처리 및 메시지

---

## 12. 예상 리소스

### 12.1 개발 시간
- 총 예상: 6-8주 (1인 개발 기준)
- Phase 1-4: 4-5주 (MVP)
- Phase 5-6: 2-3주 (추가 기능)

### 12.2 의존성
- 새로운 npm 패키지 불필요 (native fetch 사용)
- Obsidian API 활용

---

## 부록 A: API 엔드포인트

### OpenAI
```
POST https://api.openai.com/v1/chat/completions
```

### Gemini
```
POST https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent
```

### Grok (xAI)
```
POST https://api.x.ai/v1/chat/completions
```

### OpenRouter
```
POST https://openrouter.ai/api/v1/chat/completions
```

---

## 부록 B: 샘플 프롬프트

### 학습 분석 프롬프트 예시
```
당신은 학습 효율 분석 전문가입니다.

[역할]
학생의 학습 기록을 분석하여 효과적인 학습 전략을 제안합니다.

[분석 데이터]
기간: 2026-01-13 ~ 2026-01-19
총 학습 세션: 12회
총 학습 시간: 15시간 30분

세션 상세:
- 2026-01-13: 수학 (2시간), 영어 (1시간)
- 2026-01-14: 과학 (1.5시간)
...

[요청사항]
다음 항목을 포함한 주간 학습 리포트를 작성해주세요:
1. 학습 패턴 분석
2. 과목별 시간 투자 분석
3. 강점과 개선점
4. 다음 주 학습 추천

[출력 형식]
Markdown 형식, 한국어로 작성
```
